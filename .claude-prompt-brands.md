Major rewrite of the /for-brands page (components/for-brands-content.tsx). The current page is too thin and the step preview cards show empty 'Preview' placeholders.

## Issues to fix:

### 1. Step previews don't load
The 'Three steps' section has placeholder divs saying 'Preview'. Replace with rich illustrated cards with icons and real content.

### 2. Make the page much more detailed

Rewrite the entire for-brands-content.tsx with these sections:

#### A. Hero (keep similar but polish)
- Same messaging but add a stat line like '500+ creators ready to work'
- Keep both CTAs

#### B. How it Works - Rich step cards
Clean white cards with large numbered steps. Each step should have:
- Large step number (01, 02, 03)
- Title
- 3-4 bullet points of detail
- A relevant SVG icon (not a screenshot)

Steps:
1. Search and Discover - Filter by niche, platform, audience size, location, engagement rate, budget. See portfolios and reviews before you reach out.
2. Book and Pay - Select a service, submit your brief, pay securely via Stripe. Funds held in escrow until you approve deliverables.
3. Receive and Review - Get deliverables on schedule with full usage rights. Approve and release payment. Leave a review.

#### C. AI Agents and API Section (NEW - detailed)
Key differentiator. Create a prominent section:

Title: 'Your AI agent can hire creators too'
Subtitle: 'HireACreator is the first creator marketplace with a full API and MCP server. Your AI marketing agent can search, filter, vet, and book creators autonomously.'

Show three integration methods as cards:
1. REST API - Full REST API with search, profiles, bookings, and webhooks. $49/mo for 1000 req/min.
2. MCP Server - Model Context Protocol server for Claude, GPT, and other AI agents. One-line integration.
3. Webhooks - Real-time notifications for bookings, deliveries, reviews, and payments.

Add a code snippet showing a sample MCP call in a dark code block:
```json
{
  "tool": "search_creators",
  "input": {
    "niche": "UGC",
    "location": "Australia",
    "max_rate": 500,
    "min_rating": 4.5
  }
}
```

Link to /api page for full docs.

#### D. For Agencies section
- Manage multiple campaigns from one dashboard
- Invite team members with role-based access
- Track all creator relationships and spending
- Bulk outreach tools
- White-label marketplace option for enterprise

#### E. Pricing snapshot
Show Brand Analytics ($199/mo) and Enterprise ($999/mo) cards briefly with a link to /pricing for full details.

#### F. Trust and Security section
- Stripe escrow payments
- Verified creator profiles
- Real brand reviews
- Contract management
- Dispute resolution

#### G. Contact Form (NEW)
Add a contact form at the bottom for enterprise inquiries:
- Company name (required)
- Email (required)
- Monthly creator budget (dropdown: Under $5K, $5K-$20K, $20K-$50K, $50K+)
- Message (textarea)
- Submit button

The form should POST to /api/contact. Create this endpoint at app/api/contact/route.ts:
- Validate inputs
- Send email via Resend to inpromptyou@gmail.com
- Return success response
- Show success message in UI after submit

Use Resend like this (check existing code for the pattern):
```ts
import { Resend } from 'resend';
const resend = new Resend(process.env.RESEND_API_KEY);
```

#### H. Final CTA
Dark bg section with Browse Creators + Contact Sales buttons.

### 3. Animations
Use existing AnimateOnScroll and StaggerChildren components throughout.

### 4. Design
- Monochrome (black/white/neutral) design
- No blue gradients
- Use font-display and font-sans classes
- Professional, clean, data-driven feel
- Cards with subtle hover effects

When completely done with ALL changes, run these two commands:
git add -A && git commit -m "feat: comprehensive for-brands page rewrite" && git push
openclaw system event --text "Done: for-brands page rewritten with API section and contact form" --mode now
